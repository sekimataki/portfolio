import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Asana project settings — Sangyu Xi",
  description:
    "Case study: revamping Asana’s project settings hub—cross-surface exploration and stakeholder alignment for a unified strategic direction.",
};

export default function AsanaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
